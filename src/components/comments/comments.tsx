import {Box} from 'native-base';
import React from 'react';

interface Props {
  isPost: boolean;
}

const Comments: React.FC<Props> = ({isPost}) => {
  return <Box></Box>;
};

export default Comments;
